on:
  push:
    branches:
      - main
  workflow_dispatch:

name: simple

jobs:
  ci:
    runs-on: [kirill, self-hosted, macOS, ARM64]
    steps:
      - uses: actions/checkout@v4
      - name: Env checks
        run: |
          uname -a
          env
