on: [push, workflow_dispatch]

name: simple

jobs:
  ci:
    runs-on: nscloud
    steps:
      - name: AWS CLI Version
        run: |
          aws --version
          docker buildx ls 
      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          tags: user/app:latest
